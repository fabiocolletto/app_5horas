Crie uma branch chamada PWA 5horas e deixe o app dependente das conexões de Navigation Handler API (para que links abram automaticamente no PWA instalado) e a File System Observer API para interação com arquivos locais.
